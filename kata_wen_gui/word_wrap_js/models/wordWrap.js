const wordWrap = function (str, num) {
      const arr = []
      for (const letter of str) {
            if (letter==="\\s" && str.indexOf(letter)<num) {
                  
            }
      }


      return arr.length
}



module.exports = wordWrap;